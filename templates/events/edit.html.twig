{% extends 'base.html.twig' %} {% block title %}Edit Events{% endblock %} {%
block body %}

<div class="container" width="60%">
  <h1>Edit Events</h1>

  {{ include("events/_form.html.twig", { button_label: "Update" }) }}
  <div class="btnAlign">
    <a class="btn btn-warning" href="{{ path('app_events_index') }}">go back</a>

    {{ include("events/_delete_form.html.twig") }}
  </div>
</div>
{% endblock %}
